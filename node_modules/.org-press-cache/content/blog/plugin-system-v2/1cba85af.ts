export default (async () => {
// Start minimal, add only what you need
const minimalConfig = {
  plugins: [
    ...minimalPlugins,
    domPlugin,
    CreateDrawer('AI', (d) => `<div>${d.html}</div>`),
  ],
};

})();