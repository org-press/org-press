// Source display only (not executed)
export default "describe('With Setup/Teardown', () => {\n  let testData: string[];\n\n  beforeEach(() => {\n    testData = ['a', 'b', 'c'];\n  });\n\n  afterEach(() => {\n    testData = [];\n  });\n\n  it('has initial data', () => {\n    expect(testData).toHaveLength(3);\n  });\n\n  it('can modify data', () => {\n    testData.push('d');\n    expect(testData).toHaveLength(4);\n  });\n});\n";