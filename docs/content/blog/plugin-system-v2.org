#+TITLE: Unified Plugin API v2: A Complete Redesign
#+DATE: 2026-02-04
#+AUTHOR: Org-Press Team
#+STATUS: published
#+LAYOUT: blog

* The Problem with V1

The original plugin system grew organically, accumulating complexity over time. What started as a simple way to handle code blocks evolved into a tangled web of special cases, hardcoded behaviors, and fragmented APIs.

** Pain Points

1. *Fragmented Plugin Types* - Block plugins, drawer plugins, and CLI plugins were all different interfaces
2. *Hardcoded Core Behaviors* - TOC generation, heading IDs, and syntax highlighting were baked into core
3. *Complex Configuration* - Users had to understand =plugins=, =drawerPlugins=, and internal registries
4. *Bridge Layer Hell* - The v1/v2 coexistence created a compatibility layer that satisfied no one

#+begin_src json :use excalidraw :name v1-architecture :height 500px
{
  "type": "excalidraw",
  "version": 2,
  "elements": [
    {
      "type": "rectangle",
      "id": "core",
      "x": 200,
      "y": 50,
      "width": 200,
      "height": 100,
      "strokeColor": "#c92a2a",
      "backgroundColor": "#ffc9c9",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "core-text",
      "x": 240,
      "y": 75,
      "width": 120,
      "height": 50,
      "text": "Core\n(bloated)",
      "fontSize": 20,
      "textAlign": "center",
      "strokeColor": "#c92a2a"
    },
    {
      "type": "rectangle",
      "id": "toc",
      "x": 50,
      "y": 200,
      "width": 80,
      "height": 50,
      "strokeColor": "#868e96",
      "backgroundColor": "#dee2e6",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "toc-text",
      "x": 65,
      "y": 215,
      "text": "TOC",
      "fontSize": 14,
      "strokeColor": "#495057"
    },
    {
      "type": "rectangle",
      "id": "headings",
      "x": 150,
      "y": 200,
      "width": 80,
      "height": 50,
      "strokeColor": "#868e96",
      "backgroundColor": "#dee2e6",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "headings-text",
      "x": 152,
      "y": 215,
      "text": "Headings",
      "fontSize": 14,
      "strokeColor": "#495057"
    },
    {
      "type": "rectangle",
      "id": "highlight",
      "x": 250,
      "y": 200,
      "width": 80,
      "height": 50,
      "strokeColor": "#868e96",
      "backgroundColor": "#dee2e6",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "highlight-text",
      "x": 255,
      "y": 215,
      "text": "Highlight",
      "fontSize": 14,
      "strokeColor": "#495057"
    },
    {
      "type": "rectangle",
      "id": "drawers",
      "x": 350,
      "y": 200,
      "width": 80,
      "height": 50,
      "strokeColor": "#868e96",
      "backgroundColor": "#dee2e6",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "drawers-text",
      "x": 355,
      "y": 215,
      "text": "Drawers",
      "fontSize": 14,
      "strokeColor": "#495057"
    },
    {
      "type": "text",
      "id": "hardcoded-label",
      "x": 150,
      "y": 270,
      "text": "Hardcoded in core",
      "fontSize": 12,
      "strokeColor": "#868e96"
    },
    {
      "type": "rectangle",
      "id": "block-plugins",
      "x": 50,
      "y": 320,
      "width": 120,
      "height": 50,
      "strokeColor": "#1971c2",
      "backgroundColor": "#a5d8ff",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "block-plugins-text",
      "x": 55,
      "y": 335,
      "text": "BlockPlugin[]",
      "fontSize": 14,
      "strokeColor": "#1864ab"
    },
    {
      "type": "rectangle",
      "id": "drawer-plugins",
      "x": 190,
      "y": 320,
      "width": 120,
      "height": 50,
      "strokeColor": "#2f9e44",
      "backgroundColor": "#b2f2bb",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "drawer-plugins-text",
      "x": 195,
      "y": 335,
      "text": "DrawerPlugin[]",
      "fontSize": 14,
      "strokeColor": "#2b8a3e"
    },
    {
      "type": "rectangle",
      "id": "cli-plugins",
      "x": 330,
      "y": 320,
      "width": 100,
      "height": 50,
      "strokeColor": "#e8590c",
      "backgroundColor": "#ffd8a8",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "cli-plugins-text",
      "x": 345,
      "y": 335,
      "text": "cli: {...}",
      "fontSize": 14,
      "strokeColor": "#d9480f"
    },
    {
      "type": "text",
      "id": "fragmented-label",
      "x": 130,
      "y": 390,
      "text": "3 different plugin interfaces",
      "fontSize": 12,
      "strokeColor": "#868e96"
    },
    {
      "type": "line",
      "id": "line1",
      "x": 90,
      "y": 150,
      "points": [[0, 0], [0, 50]],
      "strokeColor": "#868e96",
      "strokeWidth": 1
    },
    {
      "type": "line",
      "id": "line2",
      "x": 190,
      "y": 150,
      "points": [[0, 0], [0, 50]],
      "strokeColor": "#868e96",
      "strokeWidth": 1
    },
    {
      "type": "line",
      "id": "line3",
      "x": 290,
      "y": 150,
      "points": [[0, 0], [0, 50]],
      "strokeColor": "#868e96",
      "strokeWidth": 1
    },
    {
      "type": "line",
      "id": "line4",
      "x": 390,
      "y": 150,
      "points": [[0, 0], [0, 50]],
      "strokeColor": "#868e96",
      "strokeWidth": 1
    }
  ],
  "appState": {
    "viewBackgroundColor": "#ffffff"
  }
}
#+end_src

The diagram above shows the v1 architecture: a bloated core with hardcoded behaviors, and three separate plugin interfaces that couldn't share logic.

* Design Principles for V2

We started fresh with five guiding principles:

1. *Core is just orchestration* - Pipeline stages + plugin registration, nothing else
2. *Everything is a plugin* - TOC, headings, highlight, code blocks, drawers
3. *Simple things are simple* - Factory functions for common patterns
4. *Complex things are possible* - Full =setup(ctx)= API as escape hatch
5. *Leverage existing ecosystem* - Rehype/uniorg plugins work directly

* The New Architecture

#+begin_src json :use excalidraw :name v2-architecture :height 550px
{
  "type": "excalidraw",
  "version": 2,
  "elements": [
    {
      "type": "rectangle",
      "id": "core-v2",
      "x": 200,
      "y": 50,
      "width": 200,
      "height": 80,
      "strokeColor": "#2f9e44",
      "backgroundColor": "#b2f2bb",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "core-v2-text",
      "x": 225,
      "y": 70,
      "width": 150,
      "height": 40,
      "text": "Core (minimal)\nPipeline + Registry",
      "fontSize": 16,
      "textAlign": "center",
      "strokeColor": "#2b8a3e"
    },
    {
      "type": "rectangle",
      "id": "registry",
      "x": 200,
      "y": 160,
      "width": 200,
      "height": 60,
      "strokeColor": "#1971c2",
      "backgroundColor": "#a5d8ff",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "registry-text",
      "x": 230,
      "y": 180,
      "text": "PluginRegistry",
      "fontSize": 16,
      "strokeColor": "#1864ab"
    },
    {
      "type": "rectangle",
      "id": "context",
      "x": 200,
      "y": 250,
      "width": 200,
      "height": 60,
      "strokeColor": "#9c36b5",
      "backgroundColor": "#eebefa",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "context-text",
      "x": 225,
      "y": 270,
      "text": "PluginContextImpl",
      "fontSize": 16,
      "strokeColor": "#862e9c"
    },
    {
      "type": "rectangle",
      "id": "plugins-box",
      "x": 50,
      "y": 350,
      "width": 500,
      "height": 120,
      "strokeColor": "#495057",
      "backgroundColor": "#f8f9fa",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "plugins-label",
      "x": 250,
      "y": 355,
      "text": "OrgPressPlugin[]",
      "fontSize": 14,
      "strokeColor": "#495057"
    },
    {
      "type": "rectangle",
      "id": "plugin1",
      "x": 70,
      "y": 385,
      "width": 90,
      "height": 60,
      "strokeColor": "#1971c2",
      "backgroundColor": "#d0ebff",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "plugin1-text",
      "x": 75,
      "y": 400,
      "text": "domPlugin\n(block)",
      "fontSize": 12,
      "textAlign": "center",
      "strokeColor": "#1864ab"
    },
    {
      "type": "rectangle",
      "id": "plugin2",
      "x": 175,
      "y": 385,
      "width": 90,
      "height": 60,
      "strokeColor": "#2f9e44",
      "backgroundColor": "#d3f9d8",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "plugin2-text",
      "x": 180,
      "y": 400,
      "text": "aiDrawer\n(drawer)",
      "fontSize": 12,
      "textAlign": "center",
      "strokeColor": "#2b8a3e"
    },
    {
      "type": "rectangle",
      "id": "plugin3",
      "x": 280,
      "y": 385,
      "width": 90,
      "height": 60,
      "strokeColor": "#e8590c",
      "backgroundColor": "#ffe8cc",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "plugin3-text",
      "x": 290,
      "y": 400,
      "text": "fmtPlugin\n(command)",
      "fontSize": 12,
      "textAlign": "center",
      "strokeColor": "#d9480f"
    },
    {
      "type": "rectangle",
      "id": "plugin4",
      "x": 385,
      "y": 385,
      "width": 90,
      "height": 60,
      "strokeColor": "#9c36b5",
      "backgroundColor": "#f3d9fa",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "plugin4-text",
      "x": 395,
      "y": 400,
      "text": "rehypeHL\n(ecosystem)",
      "fontSize": 12,
      "textAlign": "center",
      "strokeColor": "#862e9c"
    },
    {
      "type": "arrow",
      "id": "arrow1",
      "x": 300,
      "y": 130,
      "points": [[0, 0], [0, 30]],
      "strokeColor": "#495057",
      "strokeWidth": 2,
      "endArrowhead": "arrow"
    },
    {
      "type": "arrow",
      "id": "arrow2",
      "x": 300,
      "y": 220,
      "points": [[0, 0], [0, 30]],
      "strokeColor": "#495057",
      "strokeWidth": 2,
      "endArrowhead": "arrow"
    },
    {
      "type": "arrow",
      "id": "arrow3",
      "x": 300,
      "y": 310,
      "points": [[0, 0], [0, 40]],
      "strokeColor": "#495057",
      "strokeWidth": 2,
      "endArrowhead": "arrow"
    },
    {
      "type": "text",
      "id": "unified-label",
      "x": 180,
      "y": 485,
      "text": "All plugins share the same interface",
      "fontSize": 14,
      "strokeColor": "#495057"
    }
  ],
  "appState": {
    "viewBackgroundColor": "#ffffff"
  }
}
#+end_src

The v2 architecture is clean and unified:
- *Core* handles only orchestration
- *PluginRegistry* loads and sorts plugins by priority
- *PluginContextImpl* provides the =setup()= API for plugins
- *All plugins* implement the same =OrgPressPlugin= interface

* Factory Functions

Instead of implementing the full =OrgPressPlugin= interface, most plugins use factory functions:

#+begin_src json :use excalidraw :name factories :height 400px
{
  "type": "excalidraw",
  "version": 2,
  "elements": [
    {
      "type": "rectangle",
      "id": "factories-box",
      "x": 100,
      "y": 50,
      "width": 400,
      "height": 300,
      "strokeColor": "#495057",
      "backgroundColor": "#f8f9fa",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "factories-title",
      "x": 220,
      "y": 60,
      "text": "Factory Functions",
      "fontSize": 18,
      "strokeColor": "#212529",
      "fontFamily": 1
    },
    {
      "type": "rectangle",
      "id": "create-drawer",
      "x": 130,
      "y": 100,
      "width": 150,
      "height": 50,
      "strokeColor": "#2f9e44",
      "backgroundColor": "#b2f2bb",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "create-drawer-text",
      "x": 150,
      "y": 115,
      "text": "CreateDrawer",
      "fontSize": 16,
      "strokeColor": "#2b8a3e"
    },
    {
      "type": "rectangle",
      "id": "create-block",
      "x": 320,
      "y": 100,
      "width": 150,
      "height": 50,
      "strokeColor": "#1971c2",
      "backgroundColor": "#a5d8ff",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "create-block-text",
      "x": 355,
      "y": 115,
      "text": "CreateBlock",
      "fontSize": 16,
      "strokeColor": "#1864ab"
    },
    {
      "type": "rectangle",
      "id": "create-transformer",
      "x": 130,
      "y": 170,
      "width": 150,
      "height": 50,
      "strokeColor": "#9c36b5",
      "backgroundColor": "#eebefa",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "create-transformer-text",
      "x": 135,
      "y": 185,
      "text": "CreateTransformer",
      "fontSize": 16,
      "strokeColor": "#862e9c"
    },
    {
      "type": "rectangle",
      "id": "create-command",
      "x": 320,
      "y": 170,
      "width": 150,
      "height": 50,
      "strokeColor": "#e8590c",
      "backgroundColor": "#ffd8a8",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "create-command-text",
      "x": 340,
      "y": 185,
      "text": "CreateCommand",
      "fontSize": 16,
      "strokeColor": "#d9480f"
    },
    {
      "type": "rectangle",
      "id": "create-plugin",
      "x": 220,
      "y": 250,
      "width": 150,
      "height": 50,
      "strokeColor": "#495057",
      "backgroundColor": "#dee2e6",
      "fillStyle": "solid",
      "strokeWidth": 1,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "create-plugin-text",
      "x": 245,
      "y": 265,
      "text": "CreatePlugin",
      "fontSize": 16,
      "strokeColor": "#212529"
    },
    {
      "type": "text",
      "id": "escape-hatch",
      "x": 225,
      "y": 310,
      "text": "(escape hatch)",
      "fontSize": 12,
      "strokeColor": "#868e96"
    }
  ],
  "appState": {
    "viewBackgroundColor": "#ffffff"
  }
}
#+end_src

| Factory | Purpose | Example Use Case |
|---------+---------+------------------|
| =CreateDrawer= | Transform org drawers to HTML | AI assistant outputs, notes, warnings |
| =CreateBlock= | Handle specific code block languages | Python, Ruby, custom DSLs |
| =CreateTransformer= | Pipeline stages for =:use= | DOM rendering, server execution |
| =CreateCommand= | CLI commands | =orgp fmt=, =orgp lint= |
| =CreatePlugin= | Full control escape hatch | Complex multi-stage plugins |

* Before and After: Configuration

** V1: Fragmented Configuration

#+begin_src typescript :use sourceOnly
// Old v1 config - multiple plugin arrays, confusing
export default {
  plugins: [javascriptPlugin, typescriptPlugin], // blocks only
  drawerPlugins: [aiDrawerPlugin],               // separate!
  // CLI plugins? Who knows where they go
};
#+end_src

** V2: Unified Configuration

#+begin_src typescript :use sourceOnly
import { defaultPlugins, aiDrawerPlugin, CreateDrawer } from 'org-press';

export default {
  plugins: [
    ...defaultPlugins,  // All core functionality
    aiDrawerPlugin,     // Optional drawer
    CreateDrawer('NOTE', (d) => `<aside class="note">${d.html}</aside>`),
  ],
};
#+end_src

* Before and After: Creating Plugins

** V1: Different Interfaces for Different Things

#+begin_src typescript :use sourceOnly
// Block plugin (v1)
const myBlockPlugin: BlockPlugin = {
  name: 'python',
  languages: ['python', 'py'],
  transform: async (block, ctx) => ({ code: block.value }),
};

// Drawer plugin (v1) - completely different interface!
const myDrawerPlugin: DrawerPlugin = {
  name: 'ai-drawer',
  drawers: ['AI'],
  transformDrawer: (drawer, ctx) => `<div>${drawer.html}</div>`,
};

// CLI plugin (v1) - embedded in BlockPlugin?!
const myCLIPlugin: BlockPlugin = {
  name: 'formatter',
  cli: {
    command: 'fmt',
    description: 'Format code',
    execute: async (args, ctx) => { /* ... */ return 0; },
  },
};
#+end_src

** V2: Consistent Factory Functions

#+begin_src typescript :use sourceOnly
import { CreateBlock, CreateDrawer, CreateCommand } from 'org-press';

// Block handler - clean factory
const pythonPlugin = CreateBlock('python', {
  languages: ['python', 'py'],
  transform: (code, ctx) => ({ html: `<pre>${code}</pre>` }),
});

// Drawer handler - same pattern
const aiDrawer = CreateDrawer('AI', (drawer) =>
  `<details class="ai"><summary>AI</summary>${drawer.html}</details>`
);

// CLI command - same pattern
const fmtCommand = CreateCommand('fmt', {
  description: 'Format code blocks',
  args: [{ name: 'check', type: 'boolean', description: 'Check only' }],
  execute: async (args, ctx) => { /* ... */ return 0; },
});
#+end_src

* The Presets System

Instead of magic configuration resolution, v2 uses simple arrays:

#+begin_src json :use excalidraw :name presets :height 350px
{
  "type": "excalidraw",
  "version": 2,
  "elements": [
    {
      "type": "rectangle",
      "id": "default-preset",
      "x": 50,
      "y": 50,
      "width": 200,
      "height": 250,
      "strokeColor": "#1971c2",
      "backgroundColor": "#e7f5ff",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "default-title",
      "x": 90,
      "y": 60,
      "text": "defaultPlugins",
      "fontSize": 16,
      "strokeColor": "#1864ab",
      "fontFamily": 1
    },
    {
      "type": "text",
      "id": "default-items",
      "x": 70,
      "y": 95,
      "text": "domPlugin\njavascriptPlugin\ntypescriptPlugin\ncssPlugin\nserverPlugin",
      "fontSize": 14,
      "strokeColor": "#495057"
    },
    {
      "type": "rectangle",
      "id": "cli-preset",
      "x": 280,
      "y": 50,
      "width": 180,
      "height": 150,
      "strokeColor": "#e8590c",
      "backgroundColor": "#fff4e6",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "cli-title",
      "x": 330,
      "y": 60,
      "text": "cliPlugins",
      "fontSize": 16,
      "strokeColor": "#d9480f",
      "fontFamily": 1
    },
    {
      "type": "text",
      "id": "cli-items",
      "x": 300,
      "y": 95,
      "text": "fmtPlugin\nlintPlugin\ntypeCheckPlugin",
      "fontSize": 14,
      "strokeColor": "#495057"
    },
    {
      "type": "rectangle",
      "id": "minimal-preset",
      "x": 280,
      "y": 220,
      "width": 180,
      "height": 80,
      "strokeColor": "#868e96",
      "backgroundColor": "#f1f3f5",
      "fillStyle": "solid",
      "strokeWidth": 2,
      "roughness": 0,
      "roundness": { "type": 3 }
    },
    {
      "type": "text",
      "id": "minimal-title",
      "x": 310,
      "y": 230,
      "text": "minimalPlugins",
      "fontSize": 16,
      "strokeColor": "#495057",
      "fontFamily": 1
    },
    {
      "type": "text",
      "id": "minimal-items",
      "x": 340,
      "y": 265,
      "text": "[ ]",
      "fontSize": 14,
      "strokeColor": "#868e96"
    }
  ],
  "appState": {
    "viewBackgroundColor": "#ffffff"
  }
}
#+end_src

#+begin_src typescript :use sourceOnly
// Use defaults - most common
const defaultConfig = {
  plugins: [...defaultPlugins],
};
#+end_src

#+begin_src typescript :use sourceOnly
// Add optional plugins
const withOptionalPlugins = {
  plugins: [...defaultPlugins, aiDrawerPlugin, noteDrawerPlugin],
};
#+end_src

#+begin_src typescript :use sourceOnly
// Start minimal, add only what you need
const minimalConfig = {
  plugins: [
    ...minimalPlugins,
    domPlugin,
    CreateDrawer('AI', (d) => `<div>${d.html}</div>`),
  ],
};
#+end_src

* Plugin Context API

For advanced use cases, =CreatePlugin= provides full access to the plugin context:

#+begin_src typescript :use sourceOnly
import { CreatePlugin } from 'org-press';

const analyticsPlugin = CreatePlugin('analytics', (ctx) => {
  // Register lifecycle hooks
  ctx.hook('build:start', () => console.log('Build started'));
  ctx.hook('build:end', (stats) => sendToAnalytics(stats));

  // Register multiple drawer handlers
  ctx.onDrawer(['NOTE', 'WARNING', 'TIP'], (drawer) =>
    `<div class="callout callout-${drawer.name.toLowerCase()}">${drawer.html}</div>`
  );

  // Register block handler with custom matching
  ctx.onBlock({
    matches: (block) => block.language === 'chart' || block.meta?.includes(':chart'),
    transform: (code, blockCtx) => ({
      html: renderChart(JSON.parse(code)),
      script: `initChart('${blockCtx.blockId}')`,
    }),
  });

  // Add CLI command
  ctx.addCommand('stats', {
    description: 'Show content statistics',
    execute: async (args, cmdCtx) => {
      const stats = await gatherStats(cmdCtx.contentDir);
      console.table(stats);
      return 0;
    },
  });
});
#+end_src

* Migration Guide

Migrating from v1 to v2 is straightforward:

** Step 1: Update imports

#+begin_src typescript :use sourceOnly
// Before
import { loadPlugins, BlockPlugin } from 'org-press';

// After
import { defaultPlugins, CreateBlock, OrgPressPlugin } from 'org-press';
#+end_src

** Step 2: Update config

#+begin_src typescript :use sourceOnly
// Before
export default {
  plugins: [...myPlugins],
  drawerPlugins: [...myDrawerPlugins],
};

// After
export default {
  plugins: [...defaultPlugins, ...myPlugins, ...myDrawerPlugins],
};
#+end_src

** Step 3: Convert custom plugins

#+begin_src typescript :use sourceOnly
// Before: v1 BlockPlugin
const myPlugin: BlockPlugin = {
  name: 'custom',
  drawers: ['CUSTOM'],
  transformDrawer: (drawer) => `<div>${drawer.html}</div>`,
};

// After: v2 factory
const myPlugin = CreateDrawer('CUSTOM', (drawer) =>
  `<div>${drawer.html}</div>`
);
#+end_src

* Summary

The v2 plugin system delivers on its promises:

| Aspect | V1 | V2 |
|--------+----+----|
| Plugin interfaces | 3 different types | 1 unified interface |
| Configuration | =plugins= + =drawerPlugins= | Single =plugins= array |
| Core behaviors | Hardcoded | All pluggable |
| Learning curve | Steep | Gradual (factories) |
| Ecosystem | Isolated | Rehype/uniorg compatible |
| Extensibility | Limited | Full =setup(ctx)= API |

The result is a plugin system that's simple for common cases, powerful for advanced cases, and consistent throughout.

#+begin_src typescript :use sourceOnly
// This is all you need for most projects
import { defaultPlugins } from 'org-press';

export default {
  plugins: [...defaultPlugins],
};
#+end_src
